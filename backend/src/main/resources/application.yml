spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/plantdb
    username: plantuser
    password: plantpass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8088
